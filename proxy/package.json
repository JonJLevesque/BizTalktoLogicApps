{
  "name": "btla-proxy",
  "version": "1.0.0",
  "description": "BizTalk to Logic Apps migration proxy — Cloudflare Worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.68.1"
  }
}
