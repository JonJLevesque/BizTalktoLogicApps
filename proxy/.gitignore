# Local dev secrets — NEVER commit
.dev.vars

# System prompt files contain proprietary domain knowledge — NEVER commit
prompts/*.txt

# Build artifacts
node_modules/
dist/

# Wrangler cache
.wrangler/
